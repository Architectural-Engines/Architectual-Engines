**Daily Log**

**ANNEAL V2 — SESSION NOTES (ARCHITECTURE SHIFT)**

Core Structural Change

**Moved from**: 
single-file monolith with implicit global dependencies

**To**:
multi-file explicit pipeline with defined input/output contracts

**New System Layers**

**Config Layer** (structure only)

state_dim, latent_dim, embed_dim, base_dim, expansion, time_feat_dim, hidden_dim, depth, layers, num_branches, epsilon.  All exist as tuneable parameters. other parameters will be found in other sections of code.

-defines shapes ONLY

-does NOT generate tensors

**Main Execution Layer**

**creates runtime tensors**:

-initial_pos

-branch_embed

-progress

-t_traj

-responsible for data generation

-passes inputs into model

**Model Layer (anneal_model.py)**
**contains**:

-time_encoder

-mlp_branch

-forward pass

-latent dynamics

-transforms inputs

owns computation graph

**Key Bugs Discovered & Fixed**

-t_feat dual-source bug

**t_feat was both**:

-passed externally
 
-computed internally
  
**FIX**: single source of truth inside model

**branch_embed dimension mismatch**

**expected**: latent_dim (132)

**actual**: base_dim (12)

**FIX**: align generator with config.latent_dim

**recursive time_encoder call from migration to multi file**

time_encoder calling itself instead of computing projection

**FIX**: removed recursive self-call, changing how time encoder is interacted with by identifying outside of def time_encoder into model.

**forward signature mismatch**
model(args) != forward(args)

**FIX**: standardized input contract

**missing runtime variables (initial_pos, t_traj), error as a result of migration, showing mismatches in ownership**

assumed global existence instead of explicit creation

**FIX**: moved into main/data pipeline

**Major Insight (IMPORTANT)**

**Before**:

implicit globals + monolith assumptions

**After**:

explicit data-flow graph with strict tensor ownership

**Dimensional Collapse Discovery**
**Intended**:
base_dim → expansion → latent_dim

**Actual bug found**:
branch_embed still in base_dim space (12D)
**FIX**: generate or project into latent_dim space (132D)

**Key Takeaway 60% of code is migrated into multiple files**

-config defines structure

-main defines data

-model defines transformation

Debugging Evolution
progressed through:

import errors

scope errors

signature mismatch

tensor mismatch

recursion bug

representation mismatch

→ now at architecture correctness stage

**Final System State**
-multi-file split successful 3 of the files exist and are fully fuctional within scope

-forward pass executes

-time encoding stabilized

-model instantiates correctly

**summary**:
Anneal transitioned from monolithic implicit state to modular explicit tensor graph with strict dimensional contracts

****UPON RELEASE ANNEAL V2 will exist as 7, or 8 python files****
