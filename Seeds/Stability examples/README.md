**Seed generator Visuals**

Contains 4 stability examples per image, for each movement type
  -2 "unstable" seeds demonstrating failure tendencies
  -2 "stable" seeds demonstrating desireable trajectory patterns

**Purpose**: Visual reference only;  stability behavior emerges through experimentation.

The seed chosen will directly change the process of tuning, stability, pruning, and usecase.
